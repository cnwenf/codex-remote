# 图片发送链路保护

- 用户明确要求：保留 v0.5.35 已可发送图片的逻辑，以后不要因图片下载、预览或 Markdown 显示问题修改发送链路。
- Android 原生端使用聊天 WebSocket 上传图片；不要改回 HTTP 上传，也不要自动切换上传协议。
- 保护入口：`src/mobile/mobile-shell.tsx` 中的传输选择、`src/web/state/use-codex.ts` 中的上传与发送顺序、`src/web/api/socket.ts` 中的图片上传，以及对应 Gateway 接收逻辑。
- 除非用户再次明确授权修改发送逻辑，否则保持上述行为。用户报告退化时，先对照基线并复现；若没有代码差异，必须如实说明，不能制造回退或把下载错误当成上传错误。
