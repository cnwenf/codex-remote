# 任务通知与连接状态分离验收 — 2026-09-09

## 当前状态

0.5.34 候选已完成开发、自动化回归与独立代码审查，**尚未发布**。Mac 锁屏阻止本轮 Android / iPhone 页面实际点击；不绕过锁屏，不把自动化和构建通过写成原生页面验收通过。

本轮只处理任务通知的展示与状态归属。红米真机私网请求超时根因仍缺设备证据，图片发送问题按用户要求延期；不宣称这两项已解决。既有 Mac Gateway 0.5.32 保持运行，没有重启 Desktop 或替换本机网关。

## 最终交互约定

- 聊天连接正常就显示绿色，包括任务通知延迟、失败或通知权限关闭的情况。
- 对话页和连接列表移除任务通知大块警告及重试按钮；通知只影响设置页，不驱动连接颜色。
- 连接中 / 重连中为灰色空心点，实际离线为红色 ×；原生横竖屏使用同一标识。连接胶囊仍打开该连接的会话列表。
- 设置页使用“任务通知”和“重新检查”，展示对应连接、真实最近成功时间、连续失败次数和可识别的失败原因。
- 点击重新检查不代表检查成功；原生真实成功后才更新成功时间、清空失败。说明仅检查最近打开的连接，通知受 Android 系统设置影响。

## 代码与反例

- 沿用现有设置组件和原生服务，不新增全局健康状态模型或共享状态框架。删掉原来的对话通知插槽及无用布局规则。
- 通知事实写入和连接切换由同一个原生同步边界保护，写入前校验连接 ID 与地址。实际 HTTP 反例证明：旧请求在新目标保存后、服务启动命令到达前成功，原实现会把旧成功时间记给新目标；修复后不再污染。
- 初次启动、重新检查及系统服务重启统一读取已保存目标。测试拒绝了“旧 Intent 直接早退”的中间方案，该方案可能漏掉前台服务启动；最终不使用这个分支。
- 保留既有超时 watchdog、取消旧请求、迟到响应保护、通知品牌和投递规则；图片及消息事件流生产代码未改。
- 导航测试补齐初始历史状态、等待自动恢复后才验证点击恰好一次；未放宽既有断言或修改导航生产逻辑。

## 已执行验证

| 层级 | 结果 |
| --- | --- |
| 定向组件测试 | 54 项通过；大块警告移除、离线 ×、成功时间及重新检查均先验证失败再修改 |
| 完整 TypeScript / Vitest | 68 文件 / 1011 项通过；2 GB heap、单 worker，最终执行 106.52 秒 |
| Web Playwright | 62 项通过 / 10 项按平台或专用夹具跳过；其中新横屏用例的桌面项目按规则跳过 |
| Java 单元测试 | 21 项实际重跑通过，不以 up-to-date 作为执行证据 |
| Android 原生 instrumentation | 9 项通过，63.363 秒；实际服务、HTTP、持久化与通知，不替代页面点击 |
| 已保存本机连接原生联调 | 最终候选另测 1 项通过，17.161 秒；真实网关连续两轮健康检查成功，错误清空；未导出凭据，结束后保留本机连接 |
| 独立代码审查 | Ready；已关闭串连接结果和旧启动早退问题，没有未处理 Critical / Important |
| Android 候选 | 0.5.34 / 43 debug QA 包已覆盖安装到模拟器，保留已保存连接；不是正式签名发布 APK |
| iOS Simulator 构建 | 0.5.34 / 41 构建、ZIP 完整性及本地模拟器签名校验通过；未安装本轮候选 |
| 包内 Web 一致性 | Android / iOS 候选各 7 个 Web 文件与最终 dist 逐字节一致，原生桥参数日志均关闭 |

原生 9 项包括：慢响应整体期限及自动恢复、手动重试、切换连接取消、HTTP / 非法响应分类、切换命令前的旧成功隔离、失败目标重试保留事实、旧启动命令使用最新目标、持久化事实投影、通知投递与品牌。

Android debug QA 包 SHA-256：`c853b25d2f3905a2a5052ee0d2ff5d0d3ad9b5c970363074646eee8536c1384e`。

## 发布前剩余项

- 解锁 Mac 后，在 Android 候选实际打开本机连接，检查连接列表和对话页无通知大块提示，聊天正常时保持绿点。
- 实际进入设置页，验证任务通知详情、最近成功时间和重新检查；使用受控故障确认通知异常不影响聊天页面，再恢复本机连接。
- 原生横屏页面复核。iPhone 本轮仅完成构建，不冒充页面已验收。
- 创建正式标签、等待发布流水线、重新下载并独立验证正式资产与 Android 更新清单。尚未创建 v0.5.34 标签或公开发布。
